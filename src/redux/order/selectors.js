export const selectContactData = (state) => state.order.contactData;
export const selectOrderItems = (state) => state.order.items;
export const selectLocation = (state) => state.order.delivery.location;
export const selecComment = (state) => state.order.comment;

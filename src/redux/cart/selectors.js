export const selectTotalPrice = (state) => state.cart.info.totalPrice;
export const selectCartItems = (state) => state.cart.items;
export const selectCartIsOpen = (state) => state.cart.isOpen;
export const selectCartQty = (state) => state.cart.info.qty;
